<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.vegadvisor.client"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InicioAplicacionActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".InicioSesionActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_inicio_sesion"
            android:parentActivityName=".InicioAplicacionActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".RegistroActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_registro"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vegadvisor.client.InicioAplicacionActivity"/>
        </activity>
        <activity
            android:name=".MenuPrincipalActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_menu_principal"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".EstablecimientosActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_establecimientos"
            android:parentActivityName=".MenuPrincipalActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vegadvisor.client.MenuPrincipalActivity"/>
        </activity>
        <activity
            android:name=".PerfilActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_perfil"
            android:parentActivityName=".MenuPrincipalActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vegadvisor.client.MenuPrincipalActivity"/>
        </activity>
        <activity
            android:name=".DetalleEstabActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_detalle_estab"
            android:parentActivityName=".EstablecimientosActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vegadvisor.client.EstablecimientosActivity"/>
        </activity>
        <activity
            android:name=".CrearEstabActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_crear_estab"
            android:parentActivityName=".EstablecimientosActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vegadvisor.client.EstablecimientosActivity"/>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".CercanoaMiActivity"
            android:label="@string/title_activity_cercanoa_mi">
        </activity>
    </application>

</manifest>
